<!DOCTYPE html>
<html>
  <body>
    <header>
      <h1>Welcome to Mister Pizza!</h1>
      <% @categories.each do |category| %>
        <%= link_to category.name, category_path(category) %>
      <% end %>
    </header>
  </body>
</html>
