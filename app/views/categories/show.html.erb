<!DOCTYPE html>
<html>
  <body>
    <%= @category.name %>
    <ul>
      <% @category.items.each do |item| %>
        <li>
        <%= link_to item.name, item_path(item) %>
        </li>
        <li>
        <%= item.description %>
        </li>
        <li>
        $<%= item.price/100 %>
        </li>
      <% end %>
    </ul>
  </body>
</html>


